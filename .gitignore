# Binaries (in root only)
/coi
# transitional: pre-0.10 `make build` created this symlink in existing checkouts
/claude-on-incus
*.exe
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool
*.out
coverage/
coverage.html

# Dependency directories
vendor/

# Python
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.venv/

# Build artifacts
dist/
build/
!tests/build
internal/image/embedded/
internal/config/embedded/

# Test artifacts
tmp/

# IDE
.idea/
.claude/
.vscode/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Config files (for local testing)
test-config.toml

# Session data (for local testing)

# opencode data (created when running opencode in workspace)
.local/

# pi session data (created when running pi in workspace)
.pi-sessions/

# omp session data (created when running omp in workspace)
.omp-agent/

# Backup files
*.bck.yml
*.backup

# Documentation lives in the GitHub wiki, not in-repo. This guards against
# accidentally committing local docs or process artifacts (e.g. plan/spec
# scratch from tooling) under docs/.
/docs/

# Analysis / planning scratch generated at repo root by tooling (kept local,
# never committed) — e.g. *_IMPROVEMENTS.md coverage write-ups.
/*_IMPROVEMENTS.md
